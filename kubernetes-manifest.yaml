stage('Deploy to Kubernetes') {
    steps {
        withCredentials([
            kubeconfig(credentialsId: 'kubernetes-credentials', variable: 'KUBECONFIG')
        ]) {
            sh "kubectl apply --kubeconfig=$KUBECONFIG -f kubernetes-manifest.yaml"
        }
    }
}
